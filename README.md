Steps to run project
1.Go inside Project folder
2.Open terminal in project folder
3. Run "cd backend" in terminal 
4. Run ".\venv\Scripts\activate"
5. Run "python app.py" for the backend to run
6.Right click the "dashboard.html" and open with Live Server
